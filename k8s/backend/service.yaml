apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: bookstore-dev

spec:

  type: ClusterIP

  selector:
    app: bookstore-backend

  ports:
  - port: 5555
    targetPort: 5555
