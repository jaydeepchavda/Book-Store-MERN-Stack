apiVersion: v1

kind: ConfigMap

metadata:
  name: backend-config
  namespace: bookstore-dev

data:
  PORT: "5555"

  NODE_ENV: "development"

  MONGO_HOST: "mongo"

  MONGO_PORT: "27017"